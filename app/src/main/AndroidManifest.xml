<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.liang.kotlin4android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/kotlin_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/kotlin_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".jetpack.liveData.LiveDataActivity"></activity>
        <activity android:name=".jetpack.lifecycles.LifecycleActivity" />
        <activity android:name=".jetpack.viewModel.CounterActivity" />
        <activity android:name=".activity_01.JavaActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Transparent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".broadcast.BroadcastActivity" />
        <activity android:name=".viewpager_03.TabViewPagerActivityActivity" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".MainActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".activity_01.StudyActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".widget_02.ListViewActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".widget_02.RecyclerViewActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".activity_01.SecondActivity" />
        <activity android:name=".threadNet.ThreadActivity" />
    </application>

</manifest>